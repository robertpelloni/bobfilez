# bobfilez handoff archive — 2026-04-04 session 22

## Summary
This session updated BobUI, removed the Java-port experiment, and produced a real BobUI-backed GUI probe with a precise blocker instead of guesswork.

## Delivered
- Updated `libs/bobui` from `01634f269f` to `581de545a4`.
- Removed `filez-java/`.
- Removed Java-only packaging files:
  - `scripts/package_java_msi.bat`
  - `wix/filez_java.wxs`
- Added `scripts/build_bobui_gui.bat`.
- Updated current docs and release metadata to `6.0.7`.

## BobUI finding
The BobUI-first CMake wiring is correct, but a raw BobUI source checkout is not sufficient for bobfilez GUI configure.

Observed result from the new probe script:
- BobUI source tree is detected correctly.
- Configure still fails at `find_package(Qt6 ...)`.
- Exact blocker: missing `Qt6Config.cmake` / `qt6-config.cmake`.

## Practical conclusion
Yes, BobUI can likely be made to work, but it needs to be treated as a **built/exported Qt provider**, not just a source tree sitting in `libs/bobui`.

Recommended flow:
1. build/install BobUI
2. point `BOBUI_ROOT` at the resulting prefix
3. rerun `scripts/build_bobui_gui.bat`

## Alternate-port finding
There is now no maintained alternate bobfilez port in-tree:
- Java experiment removed
- no Go port present

## Recommended next step
Focus on building/installing BobUI itself so the exported Qt6 package configs exist, then re-run the bobfilez GUI probe.
