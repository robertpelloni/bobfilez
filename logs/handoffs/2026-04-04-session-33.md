# bobfilez handoff archive — 2026-04-04 session 33

## Summary
This session converted `ExplorerWindow.qml` off `QtQuick.Controls`, making it the largest shell-adjacent surface migrated so far in the incremental controls-reduction strategy.

## Delivered
- Removed `QtQuick.Controls` usage from `gui/omni/assets/ExplorerWindow.qml`.
- Added `docs/ai/implementation/QTQUICK_CONTROLS_EXPLORER_REDUCTION.md`.
- Reconciled docs/versioning to `6.0.18`.

## Main finding
A larger shell surface can still be migrated incrementally when its stock control usage is mostly composed of:
- `Label`
- simple `Button`
- `ItemDelegate`
- stock `ScrollBar`

This is more work than the earlier shell files, but still much safer than starting with heavy panel controls.

## Measurement
Global `QtQuick.Controls` import count dropped from:
- 43 QML files

to:
- 42 QML files

## Recommended next step
Keep reducing shell-adjacent stock control usage while continuing to defer the richer control-dense panels until a better replacement strategy exists.
