# bobfilez handoff archive — 2026-04-04 session 26

## Summary
This session executed the first recommended dependency cut from the migration audit by removing the explicit WebEngine-based markdown preview path.

## Delivered
- Added native preview item:
  - `gui/omni/src/NativeMarkdownView.h`
  - `gui/omni/src/NativeMarkdownView.cpp`
- Updated markdown panel to use native preview instead of `QtWebEngine`.
- Removed `WebEngineQuick` from `gui/omni/CMakeLists.txt`.
- Updated shared GUI wiring and markdown interface comments.
- Added `docs/ai/implementation/MARKDOWN_WEBENGINE_REMOVAL.md`.
- Reconciled docs/versioning to `6.0.11`.

## Validation
- Confirmed there are no remaining `QtWebEngine`, `WebEngineView`, or `WebEngineQuick` references under `gui/`.
- Re-ran the BobUI consumer probe against the in-place BobUI build tree.
- Remaining blocker is still:
  - `Qt6Qml`
- Not `WebEngineQuick` anymore.

## Practical conclusion
The first migration cut worked exactly as intended:
- one heavyweight GUI dependency was removed
- the deeper BobUI declarative-stack blocker remains unchanged and clearer than before

## Tradeoff
The native markdown preview is lighter-weight and simpler than the former browser-oriented design, so browser-only features such as JS-based Mermaid/KaTeX/highlight.js behavior are not preserved in this phase.

## Recommended next step
Apply the same incremental strategy to `QtCharts` next, then to graphical-effects usage.
