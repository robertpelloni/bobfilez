# bobfilez handoff archive — 2026-04-04 session 42

## Summary
This session retargeted bobfilez's active native-framework path from the older BobUI/Qt-fork integration model to **BTK**.

## Delivered
- Replaced the active provider helper with `cmake/BTKFrameworkSetup.cmake`.
- Renamed native helper scripts to `scripts/build_btk_gui.bat` and `scripts/build_btk_inplace.bat`.
- Removed the BobUI-specific `OmniQmlRegistration` bootstrap dependency from `gui/omni/src/main.cpp`.
- Removed BobUI `OmniUI/omnicore` source-tree assumptions from active GUI CMake files.
- Replaced active BobUI QML imports in `PhotoDevelopPanel.qml` and `PhotoLibraryPanel.qml`.
- Added `docs/ai/implementation/BTK_PROVIDER_SWITCH.md`.
- Reconciled docs/versioning to `6.0.27`.

## Main finding
BTK is **not** a BobUI-style Qt6 drop-in provider.

It has a different package model and framework shape, so the migration had to:
- switch provider discovery to BTK/CopperSpice-style packages
- remove BobUI-specific bootstrap assumptions
- stop depending on BobUI internal source-tree layout

## Validation
- Headless build passed.
- `fo_tests.exe`: **63/63 tests passed**.

## Recommended next step
Complete and validate a real `libs/btk` population with a longer clone/build window, then probe `FO_BUILD_GUI=ON` / `FO_BUILD_OMNI=ON` against an actual BTK build/install prefix.
