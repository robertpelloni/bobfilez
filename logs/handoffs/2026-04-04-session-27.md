# bobfilez handoff archive — 2026-04-04 session 27

## Summary
This session completed the next lightweight GUI dependency reduction by removing the dead `QtCharts` import from the enhanced file-operations panel.

## Delivered
- Updated `gui/panels/EnhancedFileOpsPanel.qml` to remove `import QtCharts 2.5`.
- Added `docs/ai/implementation/QTCHARTS_REMOVAL.md`.
- Reconciled docs/versioning to `6.0.12`.

## Main finding
The panel's throughput graph was already implemented with:
- `Canvas`
- custom `onPaint` drawing
- a repaint timer

So the `QtCharts` import was no longer functional — it was simply stale dependency residue.

## Practical outcome
This is the second successful low-risk dependency cut in the phased migration plan:
1. `WebEngineQuick` removed from the markdown preview path
2. `QtCharts` removed from the enhanced file-ops panel

## Recommended next step
Target `QtGraphicalEffects` / `Qt5Compat.GraphicalEffects` next, since those imports remain widespread but are more stylistic than architectural.
