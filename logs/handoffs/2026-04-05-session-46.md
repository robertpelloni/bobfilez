# Session 46 — 2026-04-05

## Summary
Pushed the BTK investigation past the simple "Declarative is missing from the component list" hypothesis.

This session proved that even when BTK `Declarative` is experimentally re-enabled, the module is still structurally incomplete on this branch:
- stale declarative-specific CMake integration
- obsolete metatype declarations
- fatal missing QtScript-era headers such as `QtScript/qscriptvalue.h`

## What I changed

### In `libs/btk` (submodule)
- temporarily added `Declarative` to BTK's top-level `CS_OPTIONAL_COMPONENTS` to probe deeper
- temporarily modernized `src/declarative/CMakeLists.txt` just enough to get past configuration:
  - `target_add_definitions(...)` -> `target_compile_definitions(...)`
  - fixed target/resource handling to align with current target-based module patterns
- captured the resulting declarative compile failure boundary
- reverted the temporary declarative experiment changes
- restored a regressed property-name fix in:
  - `src/gui/kernel/qapplication_cs.cpp`
- committed the real BTK fix and pushed it to:
  - `origin/pi/msvc-focus-fixes-20260405`
  - commit: `59688f7b9`

### In bobfilez root
- added `docs/ai/implementation/BTK_PROVIDER_PROBE_ROUND4.md`
- bumped version to `6.0.31`
- updated:
  - `VERSION.md`
  - `core/include/fo/core/version.hpp`
  - `CHANGELOG.md`
  - `HANDOFF.md`
  - `AGENTS.md`

## Validation performed

### 1. Declarative experiment
Ran BTK in-place build with experimental `Declarative` enablement.

Observed progression:
1. initial configure failed on stale `target_add_definitions(...)`
2. after minimal CMake modernization, configuration succeeded
3. compilation then failed in declarative code on:
   - obsolete `Q_DECLARE_METATYPE(TYPE)` diagnostics
   - fatal missing `QtScript/qscriptvalue.h`

### 2. Normal BTK build restored
- reverted the temporary declarative experiment changes
- re-ran `scripts/build_btk_inplace.bat`
- confirmed the normal BTK module set remains buildable with the corrected `qapplication_cs.cpp` fix applied

## Key finding
The active blocker is now stronger than before:
- BTK does not currently export `Declarative`
- and the current `src/declarative` path is also not build-ready in this branch even if forced on

So the gap is not just package exposure or component omission.
It is deeper framework readiness.

## Recommended next step
If continuing on the BTK track, the next real work is upstream declarative revival:
- modernize `src/declarative/CMakeLists.txt`
- resolve obsolete declarative metatype usage
- determine whether QtScript/CsScript can be restored, shimmed, or retired for this module family
