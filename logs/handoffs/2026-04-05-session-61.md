# Session 61 — 2026-04-05

## Summary
Refined the BobUI restoration into a more accurate runtime model, fixed the earlier BobUI MSVC `qtmochelpers.h` stop, pushed that BobUI fix upstream after merging remote `main`, and documented that the current bobfilez GUI blocker is now an external Qt runtime/toolchain mismatch (real Qt6 QML kit exists on host, but visible desktop kit is MinGW rather than MSVC).

## Delivered
- Updated `cmake/BobUIQtSetup.cmake` to search Qt runtime roots via `QT6_ROOT` / `QT_ROOT` / `QTDIR` / `CMAKE_PREFIX_PATH` while keeping BobUI Omni wiring active.
- Updated `gui/CMakeLists.txt` and `gui/omni/CMakeLists.txt` messaging to reflect BobUI Omni wiring + Qt6 discovery.
- Updated `scripts/build_bobui_gui.bat` to warn clearly when no external Qt6 QML runtime root is supplied.
- Updated `scripts/build_bobui_inplace.bat` to disable tests/examples/benchmarks/doc-snippet intent at configure time.
- Patched `libs/bobui/src/corelib/kernel/qtmochelpers.h` to avoid the earlier MSVC generic-lambda lookup failure.
- Merged upstream BobUI `origin/main` into local `main`, preserved the MSVC fix, and pushed merged BobUI `main` successfully.
- Added `docs/ai/implementation/BOBUI_QT_RUNTIME_REALITY_CHECK_2026_04_05.md`.
- Updated root docs/metadata to `6.0.46`.

## Validation
- `scripts/build_bobui_inplace.bat`
  - now gets far beyond the old `qlocale.cpp` / `qtmochelpers.h` stop
  - later visible stop is an MSVC compiler ICE in GUI doc-snippet content via `qrangemodel_impl.h`
- inspected `libs/bobui/.github/workflows/ci_cd.yml`
  - confirmed BobUI CI installs external Qt modules including `qtdeclarative`
- inspected `D:\Qt`
  - found `Qt6QmlConfig.cmake` under `D:\Qt\6.11.0\mingw_64\...`
  - found Android kits too
  - no visible MSVC desktop Qt6 kit under `D:\Qt\6.11.0`

## Next
1. Obtain or point bobfilez at an MSVC-compatible Qt6 desktop kit with `Qml` / `Quick`.
2. Re-run the BobUI-backed GUI probe once that runtime is available.
3. Keep the BobUI qtmochelpers fix and updated gitlink in place.
