# Session 49 — 2026-04-05

## Summary
Implemented the first concrete Option C refactor in code by extracting the active native shell bootstrap into a narrower seam. This reduces direct provider coupling without pretending the BTK runtime problem is solved.

## What I changed
- added:
  - `gui/omni/src/NativeUiBootstrap.hpp`
  - `gui/omni/src/NativeUiBootstrap.cpp`
  - `docs/ai/implementation/NATIVE_UI_BOOTSTRAP_SEAM.md`
- refactored:
  - `gui/omni/src/main.cpp`
- updated build wiring:
  - `gui/CMakeLists.txt`
  - `gui/omni/CMakeLists.txt`
- bumped version to `6.0.34`
- updated:
  - `VERSION.md`
  - `core/include/fo/core/version.hpp`
  - `CHANGELOG.md`
  - `HANDOFF.md`
  - `AGENTS.md`

## Refactor effect
Before:
- `main.cpp` directly owned the active runtime path (`QGuiApplication`, `QQmlApplicationEngine`, `qmlRegisterType`, root-object failure handling, QML load)

After:
- `main.cpp` is now a minimal launcher
- runtime-specific setup moved into `fo::gui::run_omni_shell(...)`
- the bootstrap implementation contains a small internal runtime seam so future provider work can target that layer rather than the entrypoint directly

## Validation
- headless rebuild completed
- direct CLI verification shows `filez v6.0.34`
- test suite remains green: 63/63 passing

## Important note
This change does **not** make BTK usable as the active native runtime.
It only reduces coupling in the active shell bootstrap so Option C can proceed incrementally and honestly.

## Recommended next step
Continue Option C by narrowing the next provider-specific surface:
- separate QML type registration and declarative runtime selection policy from the bootstrap implementation so the runtime seam becomes easier to swap or test later
