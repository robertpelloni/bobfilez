# bobfilez handoff archive — 2026-04-04 session 36

## Summary
This session removed `QtQuick.Controls` usage from `Dashboard.qml`, completing the controls-removal effort across the entire `gui/omni/assets/` shell-assets layer.

## Delivered
- Removed `QtQuick.Controls` usage from `gui/omni/assets/Dashboard.qml`.
- Added `docs/ai/implementation/QTQUICK_CONTROLS_DASHBOARD_REDUCTION.md`.
- Reconciled docs/versioning to `6.0.21`.

## Main finding
`Dashboard.qml` proved that the migration can safely absorb a slightly richer convenience-control surface by using small local helper components instead of introducing broad abstractions too early.

## Measurement
Global `QtQuick.Controls` import count dropped from:
- 40 QML files

to:
- 39 QML files

Shell-assets milestone:
- `gui/omni/assets/` now has **0** QML files importing `QtQuick.Controls`.

## Recommended next step
Move into the routed panel layer, starting with the simplest panel candidates rather than the heaviest form/editor panels.
