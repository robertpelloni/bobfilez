# bobfilez handoff archive — 2026-04-04 session 43

## Summary
This session performed the first real BTK-native provider validation after the active framework switch.

## Delivered
- Registered `libs/btk` as a real tracked submodule.
- Ran `scripts/build_btk_inplace.bat` and confirmed BTK configures successfully before failing during its own compile in `btkinputowner.*`.
- Fixed `cmake/BTKFrameworkSetup.cmake` so bobfilez prefers the generated `libs/btk/build-btk` package config instead of the raw source-tree template.
- Ran `scripts/build_btk_gui.bat` and confirmed the next blocker is BTK build/export readiness: generated `BTKConfig.cmake` references missing companion export files for downstream use.
- Added `docs/ai/implementation/BTK_PROVIDER_PROBE.md`.
- Reconciled docs/versioning to `6.0.28`.

## Main finding
The active blocker is no longer legacy BobUI wiring.

It is now specifically:
1. BTK's own MSVC build failure in `btkinputowner.*`
2. BTK build-tree export/package readiness for downstream consumers

## Validation
- Headless build passed.
- `fo_tests.exe`: **63/63 tests passed**.

## Recommended next step
Fix or work around BTK's own build/export issues first, then re-probe bobfilez GUI / Omni against a valid BTK install/export prefix.
